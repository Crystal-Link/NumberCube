public class Test
{
    public static void main(String[] args)
    {
        /*NumberCube myCube = new NumberCube();
        int rollNum = myCube.roll();
        System.out.println(rollNum);

        NumberCube herCube = new NumberCube(20);
        rollNum = herCube.roll();
        System.out.println(rollNum);

        PairOfDice ourDice = new PairOfDice();
        rollNum = ourDice.roll();
        System.out.println(rollNum); */
        DiceProbability dpl = new DiceProbability(10000);
    }
}
